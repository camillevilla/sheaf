<h1>Your Friends</h1>
<% if @friends %>
  <ul>
  <% @friends.each do |friend| %>
    <li><%= link_to friend.name, user_copies_path(friend) %></li>
  <% end %>
  </ul>
<% end %>
<%= link_to 'Add friends', friendrequests_path %>
