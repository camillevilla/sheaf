require 'twilio-ruby'

message = "Here are your passwords!"

# puts "Who do you want to send the message to?"
# puts "A: all Salamanders"
# message_recipient = gets.chomp

# put your own credentials here
# place them here.....


# set up a client to talk to the Twilio REST API
@client = Twilio::REST::Client.new account_sid, auth_token 
# @client = Twilio::REST::Client.new(accountSID, authToken)

from = '2014643734' #Twilio given phone number

# array of numbers to send to
friends = {
  '14156092392' => 'Jim'
}

# iteration to send the messages
friends.each do | number, name |
  message = @client.account.messages.create(
      :from => from,
      :to => number,
      :body => message
    )
  puts "Sent message to #{name}." #displays in the terminal a confirmation that the message was sent
end




